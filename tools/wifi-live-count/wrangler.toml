name = "djtpcd-wifi-count"
main = "worker.js"
compatibility_date = "2026-08-28"

# Create the KV namespace first:
#   npx wrangler kv namespace create WIFI_STATE
# Then paste the returned namespace id below.
[[kv_namespaces]]
binding = "WIFI_STATE"
id = "REPLACE_WITH_KV_NAMESPACE_ID"
